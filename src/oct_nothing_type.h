#ifndef oct_nothing_type
#define oct_nothing_type

#include "oct_nothing.h"
#include "oct_primitive_types.h"

// Nothing. Nothing is not a primitive per se but does not officially have any members
OCT_DEF_PRIMITIVE(Nothing);

#endif
